# Infini-AI API keys and Claude Code routing
# Copy to ~/.omp/agent/.env (or any private path), fill in real keys, then source from ~/.zshrc:
#
#   if [[ -f "$HOME/.omp/agent/.env" ]]; then
#     set -a; source "$HOME/.omp/agent/.env"; set +a
#   fi
#
# NEVER commit this file with real keys.

# --- Infini-AI GenStudio (shared key for Claude + codex-transfer) ---
GENSTUDIO_API_KEY=sk-your-infini-ai-key-here
CODEX_TRANSFER_API_KEY=sk-your-infini-ai-key-here

# --- Claude Code → Infini-AI (not api.anthropic.com) ---
ANTHROPIC_BASE_URL=https://cloud.infini-ai.com/maas
ANTHROPIC_AUTH_TOKEN=sk-your-infini-ai-key-here
ANTHROPIC_API_KEY=sk-your-infini-ai-key-here

# Default models (override in-session with /model)
ANTHROPIC_MODEL=claude-opus-4-7
ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-7
ANTHROPIC_DEFAULT_SONNET_MODEL=glm-5.2
ANTHROPIC_DEFAULT_HAIKU_MODEL=deepseek-v4-flash

# Optional: bypass Codex bwrap sandbox (required on some shared hosts)
# HUMANIZE_CODEX_BYPASS_SANDBOX=true
